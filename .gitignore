# Build artifacts
/dist/
/web/dist/
/web/node_modules/

# Vite writes the production bundle straight here for //go:embed.
# Nothing under dist/ is tracked — `make ensure-embed` / `make frontend`
# populate it locally (throwaway .gitkeep or full UI).
internal/webui/dist/

# Local configs / runtime files
piperouter.yaml
piperouter.yaml.bak
*.log

# Local scratch area — internal notes, experiments (never published)
/playground/

# OS / editor / local agent state
.DS_Store
.idea/
.vscode/
.claude/

# Go
coverage.out
*.test
